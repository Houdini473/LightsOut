data:
  distances: [3, 5, 7]
  train_samples_per_distance: 500
  val_samples_per_distance: 125
  error_rate: 0.1
  train_seed: 42
  val_seed: 123
  use_fast_decoder: false

model:
  input_dim: 4
  hidden_dim: 128
  num_layers: 12
  dropout: 0.2

training:
  epochs: 200
  batch_size: 32
  lr: 0.001
  patience: 20

