output_dir: outputs/evaluation

evaluation:
  # distances: [3, 5, 7, 9, 11, 13]
  distances: [3, 5, 7, 9, 11, 13]

  # Error rates to sweep
  error_rates: [0.01, 0.03, 0.05, 0.07, 0.09, 0.11]
  # error_rates: [0.05, 0.08, 0.10, 0.12]
  
  # Number of samples per (distance, error_rate) point
  n_samples: 200
  
  # Maximum shots for multi-shot decoding
  max_shots: 10
  
  # Plot title
  plot_title: "RL Decoder: Failure Rate vs Physical Error Rate"